# 2D 展开图旋转动画优化清单

记录 `src/renderer/view_2d.rs` 中 F/B/L/R 旋转动画仍可改进的地方。

---

## 🔴 高优先级

### 1. 相邻面挤压方向不完整，B/L/R 视觉断裂

**问题**：`edge_slides()` 中 F 面有 4 条边完整定义，但 B/L/R 只定义了 1~2 条边：

| 旋转面 | 当前定义边数 | 应有边数 |
|--------|-------------|----------|
| F | 4 | 4 ✅ |
| B | 1 | 4 ❌ |
| L | 3 | 4 ❌ |
| R | 2 | 4 ❌ |

B 面在展开图最右侧，挤压时应该影响 B 自身的右列（远离）、R 的右列（→B）、U 的顶行右段、D 的底行右段。缺失导致 B 旋转时只有 R 在动，U/D 静止，非常突兀。

**建议**：补全 B/L/R 的 `edge_slides`，或统一用循环关系表生成，避免手动硬编码。

---

### 2. 被旋转面动画结束时有位置跳变

**问题**：`draw_rotating_face` 中 sticker 绕中心做刚体旋转，但动画结束瞬间 `apply_move` 只更新颜色矩阵，sticker 位置从旋转态（如 45°）直接跳回网格态（0°）。如果帧率不够或动画有丢帧，会看到明显的位置回弹。

**建议**：动画最后几帧做位置插值回归——让旋转角度平滑回到最近的 90° 整数倍，同时颜色已更新，视觉上只有颜色切换而无位置跳变。

---

### 3. 相邻面挤压边块缺少 wrap 回绕（没有履带循环感）

**问题**：F 旋转时 U 底行向下移动，但移出 U 面下边界的 sticker **没有** 从 F 面顶行上方重新进入。观众看到的是 3 个 sticker 向下移动后消失，而不是"循环滚动"。

对比 U/D 旋转的 `draw_band_row`，那里的 L→F→R→B 是完整循环的，F/B/L/R 也应该有类似的 wrap 效果。

**建议**：
- 被旋转面的 4 条边缘 + 相邻面的对应边缘 = 一共 12 个 sticker 组成一个"环"
- 像 U/D 条带一样做整体平移 + wrap
- 被旋转面自身的内部 4 个 sticker（中心块）保持不动或做缩放

---

### 4. B 面展开图位置导致挤压逻辑特殊

**问题**：B 面在展开图最右侧（x = 3×total_size），与 F 面（x = 1×total_size）距离很远。B 旋转时：
- B 左列 ↔ R 右列 的挤压在展开图上相距 `total_size`，视觉上像"隔空对滑"
- B 上/下边缘与 U/D 的对应行也需要处理方向反转（B 面是镜像的）

**建议**：B 面动画可以考虑用"缩放+淡出"替代"远距离平移"，或者给 B 面单独设计一套更合理的展开图偏移（如 B 放在 U 的上方？）。

---

## 🟡 中优先级

### 5. `edge_slides` 没有区分顺时针/逆时针方向

**问题**：当前 `edge_slides` 的滑动方向是固定的，逆时针旋转时相邻面应该向**反方向**滑动。

例如 F 顺时针：U 底行 ↓、R 左列 ←、D 顶行 ↑、L 右列 →  
F 逆时针：U 底行 ↑、R 左列 →、D 顶行 ↓、L 右列 ←

**建议**：`edge_slides` 接收 `clockwise: bool` 参数，或返回双向定义，`draw_face_with_slides` 根据旋转方向选择/反转。

---

### 6. 被旋转面高亮边框丢失

**问题**：旧实现中 `is_rotating` 的面有金色高亮边框 (`Color32::from_rgb(255, 255, 100 + ...)`)，新 `draw_rotating_face` 中只有固定的 `Stroke::new(2.0, Color32::from_rgb(255, 255, 100))`，没有动态 glow 效果。

**建议**：在 `draw_rotating_face` 中根据 `progress` 恢复动态边框光晕。

---

### 7. 相邻面内部 6 个 sticker 被重复绘制

**问题**：`draw_face_with_slides` 绘制整个 3×3 面，但只有边缘行/列在动，内部 6 个 sticker 位置完全不变。无动画时 `draw_face_plain` 已经画过全部面了。

**建议**：
- 动画期间：先用 `draw_face_plain` 画相邻面的**内部区域**（跳过边缘行/列）
- 再用 `draw_face_with_slides` 只画**边缘行/列**
- 减少无效绘制

---

### 8. 2D 与 3D 动画节奏不一致

**问题**：3D 视图使用 `sin(progress * π)` 缓动（加速-减速），2D 的 F/B/L/R 面旋转目前是线性角度。两者播放同一个 `Move` 时节奏不同步。

**建议**：统一使用 `elastic = sin(progress * π)` 作为 `angle` 和 `slide_offset` 的缓动函数。

---

## 🟢 低优先级 / 增强

### 9. L 面旋转时 U/D 行的分段处理

**问题**：L 面旋转时，U 底行和 D 顶行理论上只有**左端 1 个 sticker** 参与（因为 L 面只与 U/D 的左列相邻），但展开图上 U 底行和 D 顶行是完整一行。

当前实现让整行都滑动，虽然视觉上可接受，但严格来说只有左段应该动。

**建议**：如果追求精确，可以用单个 sticker 级别的滑动，而不是整行/整列。

---

### 10. 被旋转面中心块可以加缩放脉冲

**问题**：旧实现中被旋转面有 `face_scale = 1.0 - elastic * 0.05` 的呼吸缩放效果，新实现移除了。

**建议**：在 `draw_rotating_face` 中给中心块（或全部 9 个 sticker）加上轻微的缩放脉冲，增强"被操作"的反馈感。

---

## 总结：理想的 F 面旋转动画流程

```
1. 相邻面内部 6 个 sticker：静态绘制
2. 相邻面边缘 sticker：向 F 面方向平移 progress * step
   - U 底行 ↓，R 左列 ←，D 顶行 ↑，L 右列 →
   - 移出边界的 sticker 从 F 面对应边缘 wrap 进入
3. F 面内部 4 个 sticker（中心十字）：轻微缩放脉冲
4. F 面边缘 8 个 sticker + 中心 1 个：整体绕面中心旋转 angle
5. 动画结束：apply_move 更新颜色，位置瞬间回归网格
```

